---

### ✅ Обновлённый `README.md`

```markdown
# Oracle → Parquet → Greenplum: демо-проект миграции данных

> **Цель**: продемонстрировать сквозной процесс миграции из Oracle в аналитическую СУБД через Parquet, выявить типичные проблемы и предложить решения.  
> Проект сфокусирован на **практической реализации**, а не теории — всё можно запустить локально.

##  Исходная схема (Oracle XE 21c)

Создана в Docker-контейнере с использованием PDB (`XEPDB1`). Включает:

- **`customers`**: `id` (NUMBER), `full_name` (VARCHAR2), `email`, `created_at` (DATE)
- **`accounts`**: `id`, `customer_id` (FK → customers), `balance` (NUMBER), `currency`, `status`, `opened_at`
- **`transactions`**: `id`, `from_account`, `to_account`, `amount` (NUMBER), `description`, `trans_date`
- **`audit_log`**: `log_id`, `action`, `details` (VARCHAR2 до 4000), `logged_at`

**Enterprise-элементы**:
- Внешние ключи
- Последовательности + триггеры автоинкремента
- Индексы по связанным полям
- PL/SQL: функция `get_balance()` и процедура `transfer_funds()`

##  Ключевые проблемы миграции и решения

| Элемент | Проблема | Решение |
|--------|---------|--------|
| **Типы данных** | `NUMBER(p,s)` → неоднозначное отображение | Явное преобразование: целые → `BIGINT`, с плавающей точкой → `NUMERIC` |
| **Дата/время** | `DATE` в Oracle = дата+время, но без микросекунд | Конвертация в `TIMESTAMP` в Parquet/PostgreSQL |
| **Строки большой длины** | `VARCHAR2(4000)` — близко к лимиту | Безопасно сохраняется в Parquet как `string` |
| **Автоинкремент** | Oracle: `SEQUENCE` + триггер | В Greenplum: `IDENTITY` или генерация на стороне ETL |
| **PL/SQL-логика** | Нет прямого аналога в PostgreSQL | Переносится как отдельные функции на PL/pgSQL или выносится в приложение |
| **Схема (PDB)** | Таблицы создаются в CDB, а не в PDB | Явное указание: `ALTER SESSION SET CONTAINER = XEPDB1` |

##  Как запустить

1. Запустите Oracle XE:
   ```bash
   docker-compose up -d
   ```
2. Дождитесь полной инициализации (~10 мин):
   ```bash
   docker logs -f oracle-xe  # ждите "DATABASE IS READY TO USE!"
   ```
3. Настройте Python-окружение:
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   pip install --upgrade pip
   pip install oracledb pandas pyarrow  # Instant Client не требуется (thin mode)
   ```
4. Выполните выгрузку:
   ```bash
   python extract/extract_to_parquet.py
   ```
   → Создадутся файлы: `customers.parquet`, `accounts.parquet`, `transactions.parquet`, `audit_log.parquet`.
    файлы можно без труда переместить в Greemplum или аналитическую систему. 

## 💡 Почему Parquet?

- Сохраняет схему и типы
- Компактен и эффективен для аналитики
- Универсален: поддерживается Spark, Trino, DuckDB, Pandas
- Позволяет провалидировать данные **до** загрузки в целевую систему

## 📌 Ограничения Oracle XE

- Нет партиций, materialized views, AQ, CDC
- Но достаточно для демонстрации **90% типовых задач миграции**:
  - Преобразование типов
  - Обработка FK и последовательностей
  - Миграция бизнес-логики


> Автор: Илья  Нашахалов



